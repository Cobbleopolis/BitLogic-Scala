apply plugin: "java"
apply plugin: "scala"

sourceCompatibility = 1.7
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets {
    main {
        java {
            srcDirs = [ "src/java" ]
        }
        scala {
            srcDirs = [ "src/scala" ]
        }
    }
}


eclipse.project {
    name = appName + "-core"
}

dependencies {

}